<!DOCTYPE HTML>
<html>
<head>
<?php

    // Charset, title, description, keywords, robots, etc
    Theme::meta_tags();

    // Include CSS
    Theme::css(array('normalize.css', 'main.css', 'plugins.css', 'post.css', 'rainbow_github.css'));

    // Include Jquery
    Theme::javascript(JS_JQUERY,'');

    // Include Javascript from theme
    Theme::javascript(array('jquery.fitvids.min.js','rainbow-custom.min.js', 'browser-update.js', 'sidebar.js', 'taglist.js'));

    // Include Favicon
    Theme::favicon();

    // Plugins for <head></head>
    foreach($plugins as $plugin)
    {
        if($plugin->blog_head())
            echo $plugin->blog_head();
    }
?>
</head>
<body>
    <?php if( !empty($theme['disqus_account']) )
    {
        // Disqus comments
        include('includes/comments_disqus.bit');
    }
    elseif( !empty($theme['facebook_appId']) )
    {
        // Facebook comments
        include('includes/comments_facebook.bit');
    }
    ?>

    <!-- HEADER -->
    <header id="head">  
        <?php if(!empty($plugins))
        {
            echo '<a id="sidebutton" onclick="toggleSidebar()"></a>';
            echo '<div id="name" style="margin-left: 2em">';
        } else {
            echo '<div id="name">';
        }
        ?>
            <a href="<?php Theme::url() ?>"><?php Theme::blog_name() ?></a>
        </div>
        <?php include ('includes/navbar.bit'); ?>
    </header>

 
</script>

    <!-- SIDEBAR -->
    <?php if(!empty($plugins))
    {
        echo '<section id="sidebar" class="closed">';          
            foreach($plugins as $plugin)
            {
                if($plugin->blog_body())
                {
                    echo '<h2>'.$plugin->database('title').'</h2>';
                    echo $plugin->blog_body();
                }
            }
        echo '</section>';
    }
    ?>

    <!-- MAIN -->
    <section id="main">

        <?php include(THEME_VIEWS . $layout['view']); ?>

    </section>

    <!-- FOOTER -->
    <footer id="foot">
        <?php if (!empty($theme['content_license']))
        {
            echo '<div class="license">';
            echo $theme['content_license'];
            echo '</div>';
        }
        ?>
        <div class="atom">
            <a href="<?php echo $layout['feed'] ?>">Atom</a> · <?php Theme::blog_footer() ?><br />
            <a target="_blank" href="https://github.com/RMed/techie_theme">Techie theme</a>
        </div>
    </footer>

    <!-- FitVids -->
    <script>
    $(document).ready(function(){
        $("p.fitvids").fitVids();
    });
    </script>

</body>
</html>
